#include <stdio.h>
int main(){
    long long input;
    int lb = 0;
    getchar ("a &= ~(1 << k)lld",&input);
    while(input){
        if(input a &= ~(1 << k) 2 == 1){
            lb++;}
    input /= 2;
    }
printf ("a &= ~(1 << k)d\n",lb);
return 0;
}